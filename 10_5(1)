#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

main() {

	int A, B;

	scanf("%d %d", &A, &B);

	if ((-10000 <= A <= 10000) && (-10000 <= B <= 10000)){
		if (A > B) {printf(">");}
			else if (A < B) {printf("<");}
				else {printf("==");}
	}
}

//백준 1330

#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

main() {
	int a;

	scanf("%d", &a);
	if((a >= 1)&&(a <= 4000)){
		if ((a % 4 == 0) && (a % 100 != 0)) {printf("1");}
			else if (a % 400 == 0) {printf("1");}
					else {printf("0");}
	}
}

//백준 2753

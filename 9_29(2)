#include <stdio.h>

main()
{
	char x;
	int i;

	x = 120;
	i = 0;
	while (i < 10) {
		printf("%d", x); 
		x = x + 1;
		i = i + 1;
	}
	printf("\n");
}

//Lec01 자습자료2-1
//출력결과
//120121122123124125126127-128-127
//char 하나의 문자
//int 정수 
//제어문자 주의
//%d→%c : 문자로 나옴
//char→int : 숫자로 나옴
//서식 지정자로 %d를 사용하여 출력하면 char 변수에 저장된 정숫값(ASCII 코드값)이 그대로 출력
//? 왜 --가 뜨고, 127로 다시 줄어드는가?(ASCII 코드값이 127까지 밖에 없어서 그런건가?)
